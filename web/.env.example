# Next.js 前端环境变量示例。复制为 .env.local 后按需修改；.env.local 不入库。
#
# NEXT_PUBLIC_API_BASE_URL：后端 API 基址，前端所有请求都拼在它前面。
#   - 本地开发推荐使用 "/api"，配合 next.config.ts 的 dev rewrites 把 /api 代理到后端 4000，
#     既不暴露后端端口，也规避 Next(3000) 与后端(4000) 的跨域问题。
#   - 生产环境改为 https://api.shujingspace.com/api 或由同域反代 /api。
NEXT_PUBLIC_API_BASE_URL=/api
