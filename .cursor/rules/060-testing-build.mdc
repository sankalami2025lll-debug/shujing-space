---
description: 
alwaysApply: true
---

每次完成一个页面或功能后必须检查：
1. `pnpm install` 已完成依赖安装。
2. `pnpm build` 能通过。
3. 页面没有明显 TypeScript 错误。
4. 导航跳转没有断链。
5. 弹窗能打开、关闭，Escape 关闭逻辑可用时不报错。
6. 表单提交有 loading、成功、失败提示。
7. 移动端至少不出现横向严重溢出。
8. 中文文案没有乱码、错别字、伪英文。

Agent 修改代码前必须先说明计划；修改后必须说明：
- 修改了哪些文件
- 完成了什么
- 如何本地验证
- 是否还有待补充项
