# 前端（Vite 原型）环境变量示例。复制为 .env.local 后按需修改；.env.local 不入库。
#
# VITE_API_BASE_URL：后端 API 基址，前端所有请求都拼在它前面。
#   - 本地开发推荐使用 "/api"，配合 vite.config.ts 的 dev proxy 把 /api 代理到后端 4000，
#     既不暴露后端端口，也规避 Vite(5173) 与后端(4000) 的跨域问题。
#   - 如需前端直连后端（不走代理），可改为 http://localhost:4000/api，
#     此时需确保后端 CORS_ORIGIN 放行了前端来源（默认已含 http://localhost:5173）。
VITE_API_BASE_URL=/api
